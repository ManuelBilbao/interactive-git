{
  "name": "git-interactivo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Sitio interactivo para aprender los comandos basicos de git",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.js && npm run check:render",
    "check:render": "node scripts/render-check.mjs",
    "check:browser": "node scripts/browser-check.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
